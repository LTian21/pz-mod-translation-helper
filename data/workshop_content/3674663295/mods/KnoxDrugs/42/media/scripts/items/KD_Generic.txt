module KD
{
  item BaggieEmpty
  {
		ItemType = base:normal,
		DisplayCategory = Container,
		Weight = 0.05,

		Icon = BaggieEmpty,
		Tooltip = Tooltip_KD_Baggie,
  }

  item Ammonia
  {
    ItemType = base:drainable,
    DisplayCategory = Household,
    Weight = 1.0,
    UseDelta = 0.1,
      
    Icon = Ammonia,
  }

  item AcidBottle
  {
    ItemType = base:drainable,
    DisplayCategory = Household,
    Weight = 1.0,
    UseDelta = 0.1,

    Icon = AcidBottle,
  }

    -- item Syringe
    -- item Grinder
    -- item AcidBottle
    -- item Ammonia
    -- item Baggie
}