/* Workaround (B42.13.1 MP):
 * CraftRecipe must currently be defined in Base module due to engine issue.
 * TODO: Move back to custom module (P4PrismPower) once fixed upstream.
 */
module Base
{
	imports
	{
		Base
	}

	craftRecipe P4MakeupEyeshadowRefill
	{
		timedAction = P4PrismPower.P4MakeupEyeshadow,
		time = 40,
		/* OnTest = P4PrismPowerRecipes.OnTest.MakeupEyeshadowRefill, */
		OnCreate = P4PrismPowerRecipes.OnCreate.MakeupEyeshadowRefill,
		Tags = InHandCraft,
		category = Packing,
		inputs
		{
			item 1 [P4PrismPower.P4MakeupEyeshadow] mode:keep flags[NotFull;AllowFavorite],
			item 1 [Base.MakeupEyeshadow;Base.MakeupFoundation;Base.Lipstick] mode:keep flags[NotEmpty],
		}
		outputs
		{
			/* all items are kept */
		}
	}
}
