module LDK
{
	imports
	{
		Base
	}

    item Lokal
	{
		DisplayName = Lokal,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = Lokal,
		EatType = popcan,
		StaticModel = Lokal_Ground,
		WorldStaticModel = Lokal_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedBeverageCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canLokal,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Beer:1.0,
            		}
        	}
    }
    
    item Skol
	{
		DisplayName = Skol,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = Skol,
		EatType = popcan,
		StaticModel = Skol_Ground,
		WorldStaticModel = Skol_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedBeverageCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canSkol,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Beer:1.0,
            		}
        	}
    }

    item Itaipava
	{
		DisplayName = Itaipava,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = Itaipava,
		EatType = popcan,
		StaticModel = Itaipava_Ground,
		WorldStaticModel = Itaipava_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedBeverageCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canItaipava,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Beer:1.0,
            		}
        	}
    }
    
    item Brahma
	{
		DisplayName = Brahma,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = Brahma,
		EatType = popcan,
		StaticModel = Brahma_Ground,
		WorldStaticModel = Brahma_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedBeverageCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canBrahma,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Beer:1.0,
            		}
        	}
    }

    item Antarctica
	{
		DisplayName = Antarctica,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = Antarctica,
		EatType = popcan,
		StaticModel = Antarctica_Ground,
		WorldStaticModel = Antarctica_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedBeverageCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canAntarctica,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Beer:1.0,
            		}
        	}
    }

    item AntarcticaSubZero
	{
		DisplayName = AntarcticaSubZero,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = AntarcticaSubZero,
		EatType = popcan,
		StaticModel = AntarcticaSubZero_Ground,
		WorldStaticModel = AntarcticaSubZero_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedBeverageCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canAntarcticaSubZero,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Beer:1.0,
            		}
        	}
    }

    item Schin
	{
		DisplayName = Schin,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = Schin,
		EatType = popcan,
		StaticModel = Schin_Ground,
		WorldStaticModel = Schin_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedBeverageCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canSchin,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Beer:1.0,
            		}
        	}
    }

    item Bavaria
	{
		DisplayName = Bavaria,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = Bavaria,
		EatType = popcan,
		StaticModel = Bavaria_Ground,
		WorldStaticModel = Bavaria_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedBeverageCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canBavaria,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Beer:1.0,
            		}
        	}
    }

    item Bohemia
	{
		DisplayName = Bohemia,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = Bohemia,
		EatType = popcan,
		StaticModel = Bohemia_Ground,
		WorldStaticModel = Bohemia_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedBeverageCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canBohemia,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Beer:1.0,
            		}
        	}
    }

    item Kaiser
	{
		DisplayName = Kaiser,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = Kaiser,
		EatType = popcan,
		StaticModel = Kaiser_Ground,
		WorldStaticModel = Kaiser_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedBeverageCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canKaiser,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Beer:1.0,
            		}
        	}
    }

    item Crystal
	{
		DisplayName = Crystal,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = CrystalCan,
		EatType = popcan,
		StaticModel = CrystalCan_Ground,
		WorldStaticModel = CrystalCan_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedBeverageCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canCrystal,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Beer:1.0,
            		}
        	}
    }

    item Itaipava0
	{
		DisplayName = Itaipava0,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = Itaipava0,
		EatType = popcan,
		StaticModel = Itaipava0_Ground,
		WorldStaticModel = Itaipava0_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedBeverageCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canItaipava0,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = NoAlcoholBeer:1.0,
            		}
        	}
    }

    item Bavaria0
	{
		DisplayName = Bavaria0,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = Bavaria0,
		EatType = popcan,
		StaticModel = Bavaria0_Ground,
		WorldStaticModel = Bavaria0_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedBeverageCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canBavaria0,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = NoAlcoholBeer:1.0,
            		}
        	}
    }

    item Brahma0
	{
		DisplayName = Brahma0,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = Brahma0,
		EatType = popcan,
		StaticModel = Brahma0_Ground,
		WorldStaticModel = Brahma0_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedBeverageCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canBrahma0,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = NoAlcoholBeer:1.0,
            		}
        	}
    }

	item SkolBeats150
	{
		DisplayName = Skol Beats 150 BPM,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.1,
		Icon = SkolBeats150Bpm,
		EatType = popcan,
		StaticModel = SkolBeats150_Ground,
		WorldStaticModel = SkolBeats150_Ground,
		FillFromDispenserSound = GetWaterFromDispenserGlass,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapGlass,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = CookableMicrowave,

        	component FluidContainer
        	{
            	ContainerName   = SkolBeats150Beer,
            	capacity        = 0.2,
				Opened	= true,
				CustomDrinkSound = DrinkingFromBottleGlass,

            	Fluids
            		{
                	fluid           = Beer:1.0,
            		}
        	}
	}
}