module LDK
{
	imports
	{
		Base
	}
    
    item CocaCola
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = CocaCola,
		EatType = popcan,
		StaticModel = CocaCola_Ground,
		WorldStaticModel = CocaCola_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan;ldk:canBePacked,   
    
            component FluidContainer
        	{
            	ContainerName   = canCocaCola,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Cola:1.0,
            		}
        	}
    }

    item Itubaina
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = Itubaina,
		EatType = popcan,
		StaticModel = Itubaina_Ground,
		WorldStaticModel = Itubaina_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canItubaina,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Guarana:1.0,
            		}
        	}
    }
    
    item ItubainaZero
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = ItubainaZero,
		EatType = popcan,
		StaticModel = ItubainaZero_Ground,
		WorldStaticModel = ItubainaZero_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canItubainaZero,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = DietGuarana:1.0,
            		}
        	}
    }

    item Bare
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = Bare,
		EatType = popcan,
		StaticModel = Bare_Ground,
		WorldStaticModel = Bare_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canBare,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Guarana:1.0,
            		}
        	}
    }

    item GuaranaJesus
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = GuaranaJesus,
		EatType = popcan,
		StaticModel = GuaranaJesus_Ground,
		WorldStaticModel = GuaranaJesus_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canGuaranaJesus,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = GuaranaJesus:1.0,
            		}
        	}
    }

    item GuaranaMineiro
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = GuaranaMineiro,
		EatType = popcan,
		StaticModel = GuaranaMineiro_Ground,
		WorldStaticModel = GuaranaMineiro_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canGuaranaMineiro,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Guarana:1.0,
            		}
        	}
    }

    item Sukita
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = Sukita,
		EatType = popcan,
		StaticModel = Sukita_Ground,
		WorldStaticModel = Sukita_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan;ldk:canBePacked,    
    
            component FluidContainer
        	{
            	ContainerName   = canSukita,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaPop:1.0,
            		}
        	}
    }

    item SukitaUva
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = SukitaUva,
		EatType = popcan,
		StaticModel = SukitaUva_Ground,
		WorldStaticModel = SukitaUva_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan;ldk:canBePacked,    
    
            component FluidContainer
        	{
            	ContainerName   = canSukitaUva,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaGrape:1.0,
            		}
        	}
    }

    item CoroaCola
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = CoroaCola,
		EatType = popcan,
		StaticModel = CoroaCola_Ground,
		WorldStaticModel = CoroaCola_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canCoroaCola,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Cola:1.0,
            		}
        	}
    }

    item CoroaLaranja
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = CoroaLaranja,
		EatType = popcan,
		StaticModel = CoroaLaranja_Ground,
		WorldStaticModel = CoroaLaranja_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canCoroaLaranja,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaPop:1.0,
            		}
        	}
    }

    item CoroaLimao
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = CoroaLimao,
		EatType = popcan,
		StaticModel = CoroaLimao_Ground,
		WorldStaticModel = CoroaLimao_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canCoroaLimao,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaLime:1.0,
            		}
        	}
    }

    item CoroaUva
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = CoroaUva,
		EatType = popcan,
		StaticModel = CoroaUva_Ground,
		WorldStaticModel = CoroaUva_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canCoroaUva,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaGrape:1.0,
            		}
        	}
    }

    item EiTubaina
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = EiTubaina,
		EatType = popcan,
		StaticModel = EiTubaina_Ground,
		WorldStaticModel = EiTubaina_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canEiTubaina,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Guarana:1.0,
            		}
        	}
    }

    item FysCola
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = FysCola,
		EatType = popcan,
		StaticModel = FysCola_Ground,
		WorldStaticModel = FysCola_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canFysCola,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Cola:1.0,
            		}
        	}
    }

    item FysColaZero
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = FysColaZero,
		EatType = popcan,
		StaticModel = FysColaZero_Ground,
		WorldStaticModel = FysColaZero_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canFysColaZero,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = ColaZero:1.0,
            		}
        	}
    }
    
    item FysGuarana
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = FysGuarana,
		EatType = popcan,
		StaticModel = FysGuarana_Ground,
		WorldStaticModel = FysGuarana_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canFysGuarana,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Guarana:1.0,
            		}
        	}
    }
    
    item FysGuaranaZero
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = FysGuaranaZero,
		EatType = popcan,
		StaticModel = FysGuaranaZero_Ground,
		WorldStaticModel = FysGuaranaZero_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canFysGuaranaZero,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = DietGuarana:1.0,
            		}
        	}
    }
    
    item FysLaranja
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = FysLaranja,
		EatType = popcan,
		StaticModel = FysLaranja_Ground,
		WorldStaticModel = FysLaranja_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canFysLaranja,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaPop:1.0,
            		}
        	}
    }
    
     item FysLimao
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = FysLimao,
		EatType = popcan,
		StaticModel = FysLimao_Ground,
		WorldStaticModel = FysLimao_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canFysLimao,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaLime:1.0,
            		}
        	}
    }
    
     item FysLimaoSiciliano
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = FysLimaoSiciliano,
		EatType = popcan,
		StaticModel = FysLimaoSiciliano_Ground,
		WorldStaticModel = FysLimaoSiciliano_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canFysLimaoSiciliano,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaLime:1.0,
            		}
        	}
    }

    item FysLimaoZero
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = FysLimaoZero,
		EatType = popcan,
		StaticModel = FysLimaoZero_Ground,
		WorldStaticModel = FysLimaoZero_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canFysLimaoZero,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = DietSodaLime:1.0,
            		}
        	}
    }
    
    item Grapette
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = Grapette,
		EatType = popcan,
		StaticModel = Grapette_Ground,
		WorldStaticModel = Grapette_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canGrapette,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaGrape:1.0,
            		}
        	}
    }
    
    item GuaranaAntarctica
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = GuaranaAntarctica,
		EatType = popcan,
		StaticModel = GuaranaAntarctica_Ground,
		WorldStaticModel = GuaranaAntarctica_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan;ldk:canBePacked,    
    
            component FluidContainer
        	{
            	ContainerName   = canGuaranaAntarctica,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Guarana:1.0,
            		}
        	}
    }
    
    item GuaranaAntarcticaBlack
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = GuaranaAntarcticaBlack,
		EatType = popcan,
		StaticModel = GuaranaAntarcticaBlack_Ground,
		WorldStaticModel = GuaranaAntarcticaBlack_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canGuaranaAntarcticaBlack,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = GuaranaBlack:1.0,
            		}
        	}
    }
    
    item GuaranaCoroa
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = GuaranaCoroa,
		EatType = popcan,
		StaticModel = GuaranaCoroa_Ground,
		WorldStaticModel = GuaranaCoroa_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canGuaranaCoroa,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Guarana:1.0,
            		}
        	}
    }
    
    item GuaranaCoroaZero
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = GuaranaCoroaZero,
		EatType = popcan,
		StaticModel = GuaranaCoroaZero_Ground,
		WorldStaticModel = GuaranaCoroaZero_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canGuaranaCoroaZero,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = DietGuarana:1.0,
            		}
        	}
    }
    
    item GuaranaAntarcticaZero
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = GuaranaAntarcticaZero,
		EatType = popcan,
		StaticModel = GuaranaAntarcticaZero_Ground,
		WorldStaticModel = GuaranaAntarcticaZero_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canGuaranaAntarcticaZero,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = DietGuarana:1.0,
            		}
        	}
    }
    
    item Pepsi
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = Pepsi,
		EatType = popcan,
		StaticModel = Pepsi_Ground,
		WorldStaticModel = Pepsi_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan;ldk:canBePacked,    
    
            component FluidContainer
        	{
            	ContainerName   = canPepsi,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Cola:1.0,
            		}
        	}
    }
    
    item PepsiBlack
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = PepsiBlack,
		EatType = popcan,
		StaticModel = PepsiBlack_Ground,
		WorldStaticModel = PepsiBlack_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan;ldk:canBePacked,    
    
            component FluidContainer
        	{
            	ContainerName   = canPepsiBlack,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = ColaZero:1.0,
            		}
        	}
    }
    
    item SodaLimonada
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = SodaLimonada,
		EatType = popcan,
		StaticModel = SodaLimonada_Ground,
		WorldStaticModel = SodaLimonada_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canSodaLimonada,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaLime:1.0,
            		}
        	}
    }
    
    item SodaLimonadaZero
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = SodaLimonadaZero,
		EatType = popcan,
		StaticModel = SodaLimonadaZero_Ground,
		WorldStaticModel = SodaLimonadaZero_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canSodaLimonadaZero,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = DietSodaLime:1.0,
            		}
        	}
    }
    
    item SukitaLaranjaZero
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = SukitaLaranjaZero,
		EatType = popcan,
		StaticModel = SukitaLaranjaZero_Ground,
		WorldStaticModel = SukitaLaranjaZero_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canSukitaLaranjaZero,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = DietOrangeSoda:1.0,
            		}
        	}
    }
    
    item VivaSchinCitrus
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = VivaSchinCitrus,
		EatType = popcan,
		StaticModel = VivaSchinCitrus_Ground,
		WorldStaticModel = VivaSchinCitrus_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canVivaSchinCitrus,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaLime:1.0,
            		}
        	}
    }
    
    item VivaSchinCola
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = VivaSchinCola,
		EatType = popcan,
		StaticModel = VivaSchinCola_Ground,
		WorldStaticModel = VivaSchinCola_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canVivaSchinCola,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Cola:1.0,
            		}
        	}
    }
    
    item VivaSchinGuarana
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = VivaSchinGuarana,
		EatType = popcan,
		StaticModel = VivaSchinGuarana_Ground,
		WorldStaticModel = VivaSchinGuarana_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canVivaSchinGuarana,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Guarana:1.0,
            		}
        	}
    }
    
    item GuaranaAntarcticaBlackZero
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = GuaranaAntarcticaBlackZero,
		EatType = popcan,
		StaticModel = GuaranaAntarcticaBlackZero_Ground,
		WorldStaticModel = GuaranaAntarcticaBlackZero_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canGuaranaAntarcticaBlackZero,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = DietGuaranaBlack:1.0,
            		}
        	}
    }
    
    item ItGuaranaZero
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = ItGuaranaZero,
		EatType = popcan,
		StaticModel = ItGuaranaZero_Ground,
		WorldStaticModel = ItGuaranaZero_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canItGuaranaZero,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = DietGuarana:1.0,
            		}
        	}
    }
    
    item ItLaranja
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = ItLaranja,
		EatType = popcan,
		StaticModel = ItLaranja_Ground,
		WorldStaticModel = ItLaranja_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canItLaranja,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaPop:1.0,
            		}
        	}
    }
    
    item ItLemonIce
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = ItLemonIce,
		EatType = popcan,
		StaticModel = ItLemonIce_Ground,
		WorldStaticModel = ItLemonIce_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canItLemonIce,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaLime:1.0,
            		}
        	}
    }
    
    item ItCola
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = ItCola,
		EatType = popcan,
		StaticModel = ItCola_Ground,
		WorldStaticModel = ItCola_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canItCola,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Cola:1.0,
            		}
        	}
    }
    
    item ItGuarana
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = ItGuarana,
		EatType = popcan,
		StaticModel = ItGuarana_Ground,
		WorldStaticModel = ItGuarana_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canItGuarana,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Guarana:1.0,
            		}
        	}
    }
    
    item ItLimao
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = ItLimao,
		EatType = popcan,
		StaticModel = ItLimao_Ground,
		WorldStaticModel = ItLimao_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canItLimao,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaLime:1.0,
            		}
        	}
    }
    
    item CocaColaCafe
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = CocaColaCafe,
		EatType = popcan,
		StaticModel = CocaColaCafe_Ground,
		WorldStaticModel = CocaColaCafe_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canCocaColaCafe,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = ColaCaffeine:1.0,
            		}
        	}
    }
    
    item CocaColaZero
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = CocaColaZero,
		EatType = popcan,
		StaticModel = CocaColaZero_Ground,
		WorldStaticModel = CocaColaZero_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan;ldk:canBePacked,    
    
            component FluidContainer
        	{
            	ContainerName   = canCocaColaZero,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = ColaZero:1.0,
            		}
        	}
    }
    
    item FantaGuarana
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = FantaGuarana,
		EatType = popcan,
		StaticModel = FantaGuarana_Ground,
		WorldStaticModel = FantaGuarana_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan;ldk:canBePacked,    
    
            component FluidContainer
        	{
            	ContainerName   = canFantaGuarana,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Guarana:1.0,
            		}
        	}
    }

    item FantaGuaranaZero
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = FantaGuaranaZero,
		EatType = popcan,
		StaticModel = FantaGuaranaZero_Ground,
		WorldStaticModel = FantaGuaranaZero_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canFantaGuaranaZero,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = DietGuarana:1.0,
            		}
        	}
    }
    
    item FantaLaranja
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = FantaLaranja,
		EatType = popcan,
		StaticModel = FantaLaranja_Ground,
		WorldStaticModel = FantaLaranja_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan;ldk:canBePacked,    
    
            component FluidContainer
        	{
            	ContainerName   = canFantaLaranja,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaPop:1.0,
            		}
        	}
    }
    
    item FantaLaranjaZero
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = FantaLaranjaZero,
		EatType = popcan,
		StaticModel = FantaLaranjaZero_Ground,
		WorldStaticModel = FantaLaranjaZero_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canFantaLaranjaZero,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = DietOrangeSoda:1.0,
            		}
        	}
    }
    
    item FantaUva
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = FantaUva,
		EatType = popcan,
		StaticModel = FantaUva_Ground,
		WorldStaticModel = FantaUva_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan;ldk:canBePacked,    
    
            component FluidContainer
        	{
            	ContainerName   = canFantaUva,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaGrape:1.0,
            		}
        	}
    }
    
	item FantaUvaZero
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = FantaUvaZero,
		EatType = popcan,
		StaticModel = FantaUvaZero_Ground,
		WorldStaticModel = FantaUvaZero_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canFantaUvaZero,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = DietSodaGrape:1.0,
            		}
        	}
    }
	
	item FrukiCola
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = FrukiCola,
		EatType = popcan,
		StaticModel = FrukiCola_Ground,
		WorldStaticModel = FrukiCola_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canFrukiCola,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Cola:1.0,
            		}
        	}
    }
	
	item FrukiGuarana
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = FrukiGuarana,
		EatType = popcan,
		StaticModel = FrukiGuarana_Ground,
		WorldStaticModel = FrukiGuarana_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canFrukiGuarana,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Guarana:1.0,
            		}
        	}
    }
	
	item FrukiGuaranaZero
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = FrukiGuaranaZero,
		EatType = popcan,
		StaticModel = FrukiGuaranaZero_Ground,
		WorldStaticModel = FrukiGuaranaZero_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canFrukiGuaranaZero,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = DietGuarana:1.0,
            		}
        	}
    }
	
	item FrukiLaranja
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = FrukiLaranja,
		EatType = popcan,
		StaticModel = FrukiLaranja_Ground,
		WorldStaticModel = FrukiLaranja_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canFrukiLaranja,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaPop:1.0,
            		}
        	}
    }
	
	item Guarapan
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = Guarapan,
		EatType = popcan,
		StaticModel = Guarapan_Ground,
		WorldStaticModel = Guarapan_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canGuarapan,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Guarapan:1.0,
            		}
        	}
    }
	
	item Kuat
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = Kuat,
		EatType = popcan,
		StaticModel = Kuat_Ground,
		WorldStaticModel = Kuat_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canKuat,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Guarana:1.0,
            		}
        	}
    }
	
	item KuatZero
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = KuatZero,
		EatType = popcan,
		StaticModel = KuatZero_Ground,
		WorldStaticModel = KuatZero_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canKuatZero,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = DietGuarana:1.0,
            		}
        	}
    }
	
	item PepsiTwist
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = PepsiTwist,
		EatType = popcan,
		StaticModel = PepsiTwist_Ground,
		WorldStaticModel = PepsiTwist_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan;ldk:canBePacked,    
    
            component FluidContainer
        	{
            	ContainerName   = canPepsiTwist,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Cola:1.0,
            		}
        	}
    }
	
	item Sprite
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = Sprite,
		EatType = popcan,
		StaticModel = Sprite_Ground,
		WorldStaticModel = Sprite_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan;ldk:canBePacked,    
    
            component FluidContainer
        	{
            	ContainerName   = canSprite,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaLime:1.0,
            		}
        	}
    }
	
	item SpriteZero
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = SpriteZero,
		EatType = popcan,
		StaticModel = SpriteZero_Ground,
		WorldStaticModel = SpriteZero_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canSpriteZero,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = DietSodaLime:1.0,
            		}
        	}
    }
	
	item WewiCola
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = WewiCola,
		EatType = popcan,
		StaticModel = WewiCola_Ground,
		WorldStaticModel = WewiCola_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canWewiCola,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Cola:1.0,
            		}
        	}
    }
	
	item WewiGuarana
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = WewiGuarana,
		EatType = popcan,
		StaticModel = WewiGuarana_Ground,
		WorldStaticModel = WewiGuarana_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canWewiGuarana,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Guarana:1.0,
            		}
        	}
    }
	
	item WewiGuaranaZero
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = WewiGuaranaZero,
		EatType = popcan,
		StaticModel = WewiGuaranaZero_Ground,
		WorldStaticModel = WewiGuaranaZero_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canWewiGuaranaZero,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = DietGuarana:1.0,
            		}
        	}
    }
	
	item WewiLaranja
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = WewiLaranja,
		EatType = popcan,
		StaticModel = WewiLaranja_Ground,
		WorldStaticModel = WewiLaranja_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canWewiLaranja,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaPop:1.0,
            		}
        	}
    }
	
	item Schweppes
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = Schweppes,
		EatType = popcan,
		StaticModel = Schweppes_Ground,
		WorldStaticModel = Schweppes_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canSchweppes,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaLime:1.0,
            		}
        	}
    }
	
	item GuaranaNatu
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = GuaranaNatu,
		EatType = popcan,
		StaticModel = GuaranaNatu_Ground,
		WorldStaticModel = GuaranaNatu_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canGuaranaNatu,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Guarana:1.0,
            		}
        	}
    }
	
	item H2OH
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = H2OH,
		EatType = popcan,
		StaticModel = H2OH_Ground,
		WorldStaticModel = H2OH_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,    
    
            component FluidContainer
        	{
            	ContainerName   = canH2OH,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = SodaLime:1.0,
            		}
        	}
    }
	
	item Guaravita
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.1,
		Icon = Guaravita,
		EatType = popcan,
		StaticModel = Guaravita_Ground,
		WorldStaticModel = Guaravita_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable,   
    
            component FluidContainer
        	{
            	ContainerName   = canGuaravita,
		        Opened	= true,
            	capacity        = 0.2,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = Guarana:1.0,
            		}
        	}
    }
	
	item Toddynho
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.1,
		Icon = Toddynho,
		EatType = popcan,
		StaticModel = Toddynho,
		WorldStaticModel = Toddynho_Ground,
		Tags = Cookable,   
    
            component FluidContainer
        	{
            	ContainerName   = canToddynho,
		        Opened	= true,
            	capacity        = 0.0,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = MilkChocolate:1.0,
            		}
        	}
    }
}