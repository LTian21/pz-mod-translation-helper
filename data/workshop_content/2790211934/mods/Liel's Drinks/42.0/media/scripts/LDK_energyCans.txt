module LDK
{
	imports
	{
		Base
	}

    item RedbullMelancia
	{
		DisplayName = Redbull Watermelon,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = RedbullMelancia,
		EatType = popcan,
		StaticModel = RedbullMelancia_Ground,
		WorldStaticModel = RedbullMelancia_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedLDKEnergyCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canRedbullMelancia,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = EnergyDrink:1.0,
            		}
        	}
    }
    
    item BadWolf
	{
		DisplayName = Redbull Watermelon,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = BadWolf,
		EatType = popcan,
		StaticModel = BadWolf_Ground,
		WorldStaticModel = BadWolf_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedLDKEnergyCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canBadWolf,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = EnergyDrink:1.0,
            		}
        	}
    }

    item Fusion
	{
		DisplayName = Redbull Watermelon,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = Fusion,
		EatType = popcan,
		StaticModel = Fusion_Ground,
		WorldStaticModel = Fusion_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedLDKEnergyCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canFusion,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = EnergyDrink:1.0,
            		}
        	}
    }

    item FusionCitrus
	{
		DisplayName = Redbull Watermelon,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = FusionCitrus,
		EatType = popcan,
		StaticModel = FusionCitrus_Ground,
		WorldStaticModel = FusionCitrus_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedLDKEnergyCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canFusionCitrus,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = EnergyDrink:1.0,
            		}
        	}
    }

    item FusionGuarana
	{
		DisplayName = Redbull Watermelon,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = FusionGuarana,
		EatType = popcan,
		StaticModel = FusionGuarana_Ground,
		WorldStaticModel = FusionGuarana_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedLDKEnergyCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canFusionGuarana,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = EnergyDrink:1.0,
            		}
        	}
    }

    item Redbull
	{
		DisplayName = Redbull Watermelon,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = Redbull,
		EatType = popcan,
		StaticModel = Redbull_Ground,
		WorldStaticModel = Redbull_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedLDKEnergyCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canRedbull,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = EnergyDrink:1.0,
            		}
        	}
    }

    item RedbullAcai
	{
		DisplayName = RedbullAcai Watermelon,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = RedbullAcai,
		EatType = popcan,
		StaticModel = RedbullAcai_Ground,
		WorldStaticModel = RedbullAcai_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedLDKEnergyCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canRedbullAcai,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = EnergyDrink:1.0,
            		}
        	}
    }

    item RedbullCoco
	{
		DisplayName = RedbullCoco Watermelon,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = RedbullCoco,
		EatType = popcan,
		StaticModel = RedbullCoco_Ground,
		WorldStaticModel = RedbullCoco_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedLDKEnergyCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canRedbullCoco,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = EnergyDrink:1.0,
            		}
        	}
    }

    item RedbullPitaya
	{
		DisplayName = RedbullPitaya Watermelon,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = RedbullPitaya,
		EatType = popcan,
		StaticModel = RedbullPitaya_Ground,
		WorldStaticModel = RedbullPitaya_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedLDKEnergyCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canRedbullPitaya,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = EnergyDrink:1.0,
            		}
        	}
    }

    item RedbullTropical
	{
		DisplayName = RedbullTropical Watermelon,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
		Icon = RedbullTropical,
		EatType = popcan,
		StaticModel = RedbullTropical_Ground,
		WorldStaticModel = RedbullTropical_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;HasMetal;SealedLDKEnergyCan,   
    
            component FluidContainer
        	{
            	ContainerName   = canRedbullTropical,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = EnergyDrink:1.0,
            		}
        	}
    }
}