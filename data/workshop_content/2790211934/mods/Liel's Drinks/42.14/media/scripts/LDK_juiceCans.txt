module LDK
{
	imports
	{
		Base
	}

    item LaFruitGoiaba
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = LaFruitGoiaba,
		EatType = popcan,
		StaticModel = LaFruitGoiaba_Ground,
		WorldStaticModel = LaFruitGoiaba_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,   
    
            component FluidContainer
        	{
            	ContainerName   = canLaFruitGoiaba,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = JuiceGuava:1.0,
            		}
        	}
    }
    
    item LaFruitCaju
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = LaFruitCaju,
		EatType = popcan,
		StaticModel = LaFruitCaju_Ground,
		WorldStaticModel = LaFruitCaju_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,   
    
            component FluidContainer
        	{
            	ContainerName   = canLaFruitCaju,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = JuiceCashew:1.0,
            		}
        	}
    }
    
    item LaFruitMaracuja
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = LaFruitMaracuja,
		EatType = popcan,
		StaticModel = LaFruitMaracuja_Ground,
		WorldStaticModel = LaFruitMaracuja_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,   
    
            component FluidContainer
        	{
            	ContainerName   = canLaFruitMaracuja,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = JuicePassionFruit:1.0,
            		}
        	}
    }

    item LaFruitPessego
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = LaFruitPessego,
		EatType = popcan,
		StaticModel = LaFruitPessego_Ground,
		WorldStaticModel = LaFruitPessego_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,   
    
            component FluidContainer
        	{
            	ContainerName   = canLaFruitPessego,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = JuicePeach:1.0,
            		}
        	}
    }

    item LaFruitUva
	{
		DisplayCategory = Food,
		ItemType = base:normal,
		Weight = 0.3,
		Icon = LaFruitUva,
		EatType = popcan,
		StaticModel = LaFruitUva_Ground,
		WorldStaticModel = LaFruitUva_Ground,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = base:cookable;base:hasmetal;base:sealedbeveragecan,   
    
            component FluidContainer
        	{
            	ContainerName   = canLaFruitUva,
		        Opened	= false,
            	capacity        = 0.3,
		        CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = JuiceGrape:1.0,
            		}
        	}
    }
}