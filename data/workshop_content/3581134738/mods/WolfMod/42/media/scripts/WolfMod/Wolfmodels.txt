module Base {
    animationsMesh Wolf {
        meshFile = Skinned/Wolf,
        animationDirectory = Wolf,
        animationPrefix = Wolf_,
    }

    model Wolf {
        mesh = Skinned/Wolf,
        animationsMesh = Wolf,
        shader = animalEffect,
        static = false,
    }
}