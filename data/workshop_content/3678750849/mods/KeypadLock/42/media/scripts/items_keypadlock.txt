module Base
{
    /* =========================================================
       KeypadDevice — item required to install the keypad
       Found in electronics stores, police stations, offices.
       ========================================================= */
    item KeypadDevice
    {
        DisplayName         = Keypad Device,
        DisplayCategory     = Electronics,
        ItemType            = base:normal,
        Weight              = 0.5,
        Icon                = KeypadDevice,
        WorldStaticItem     = KeypadDevice,
        MetalValue          = 8,
        CanStoreWater       = false,
        Tooltip             = Tooltip_KeypadDevice,

        /* Loot Distribution */
        /*
            Spawned via distribution rules (see KeypadLock_Distributions.lua)
        */
    }
}
